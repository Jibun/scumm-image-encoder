You will need the following files (these can be obtained from a game resource file by using ScummPacker):
000_RMHD.dmp - stores the image dimensions
000_SMAP.dmp - stores the bitmap image data
006_CLUT.dmp - stores the palette (colour lookup table)